# Solana Gateway API base URL (dashboard calls these APIs with Bearer API key from login)
# Default is https://crypto-merchant-api.globpay.ai - override for local dev if needed
VITE_API_URL=https://crypto-merchant-api.globpay.ai
